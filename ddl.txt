CREATE TABLE MENSAGEM (ID  SERIAL NOT NULL, DATAHORA VARCHAR(255), TEXTO VARCHAR(255), CRIADOR_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE USUARIO (ID  SERIAL NOT NULL, DTYPE VARCHAR(31), ATIVO BOOLEAN, NOMESENHA VARCHAR(255), EMAIL VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE LOG (ID  SERIAL NOT NULL, DATAHORA VARCHAR(255), NOME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE USUARIO_MENSAGEM (Usuario_ID INTEGER NOT NULL, mensagens_ID INTEGER NOT NULL, PRIMARY KEY (Usuario_ID, mensagens_ID))
ALTER TABLE MENSAGEM ADD CONSTRAINT FK_MENSAGEM_CRIADOR_ID FOREIGN KEY (CRIADOR_ID) REFERENCES USUARIO (ID)
ALTER TABLE USUARIO_MENSAGEM ADD CONSTRAINT FK_USUARIO_MENSAGEM_Usuario_ID FOREIGN KEY (Usuario_ID) REFERENCES USUARIO (ID)
ALTER TABLE USUARIO_MENSAGEM ADD CONSTRAINT FK_USUARIO_MENSAGEM_mensagens_ID FOREIGN KEY (mensagens_ID) REFERENCES MENSAGEM (ID)
